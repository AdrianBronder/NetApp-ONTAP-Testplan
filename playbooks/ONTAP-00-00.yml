################################################################################
#
# Title:        NTAP-00-00.yml
# Author:       NetApp Inc. (badrian)
# Initial 
# Create Date:  2023-08-17
# Description:  Basic connection validation and cluster info
#
# URLs:         https://docs.netapp.com/us-en/ontap/index.html
#               https://galaxy.ansible.com/netapp/ontap
#
# Built-in help:
# ansible-doc netapp.ontap.na_ontap_rest_info
#
################################################################################

- hosts:              ontap
  gather_facts:       false
  vars:
    input: &input
      username:       "{{ ontap_admin_user }}"
      password:       "{{ ontap_admin_password }}"
      https:          true
      validate_certs: false
      use_rest:       "always"
  vars_files:
    - ../vars/labondemand.yml
  collections:
    - netapp.ontap

  tasks:
  - name: Print hosts and inventory source
    ansible.builtin.debug:
      msg: "{{ ansible_host }} from {{ inventory_dir }}"

  - name: Get ONTAP version info
    na_ontap_rest_info:
      gather_subset:
        - "cluster/software"
      fields:
        - "nodes.name"
        - "nodes.version"
      use_python_keys:            true
      <<: *input
      hostname:       "{{ ansible_host }}"
    register: netapp

  - name: Print ONTAP version info
    ansible.builtin.debug:
      msg:
        - "{{ netapp }}"

