################################################################################
#
# Title:        ONTAP-04-01 - Local read-only user
# Author:       NetApp Inc. (badrian)
# Initial 
# Create Date:  2023-08-17
# Description:  Cluster User Management
#               - Local read-only user
#
# URLs:         https://docs.netapp.com/us-en/ontap/index.html
#               https://galaxy.ansible.com/netapp/ontap
#
# Built-in help:
# ansible-doc netapp.ontap.<module_name>
# 
################################################################################

- hosts:              "ontap"
  name:               "ONTAP-04-01 - Local read-only user"
  gather_facts:       false
  vars:
    input: &input
      hostname:                   "{{ ansible_host }}"
      username:                   "{{ ontap_admin_user }}"
      password:                   "{{ ontap_admin_password }}"
      https:                      true
      validate_certs:             false
      use_rest:                   "always"
  vars_files:
    - ../vars/labondemand.yml
  collections:
    - netapp.ontap

  pre_tasks:

  tasks:
  - name: Create local users with read-only privileges
    netapp.ontap.na_ontap_user:
      state:                      present
      name:                       "{{ item.name }}"
      application_dicts:          "{{ item.applications }}"
      set_password:               "{{ vault_security_accounts[item.name].password }}"
      lock_user:                  "{{ item.locked }}"
      role_name:                  "{{ item.role.name }}"
      <<: *input
    loop:
      "{{ security_accounts }}"
    loop_control:
      label: "{{ item.name }}"
    when:
      - security_accounts | length > 0
      - item.name is defined
      - item.role.name == "admin"

  post_tasks: