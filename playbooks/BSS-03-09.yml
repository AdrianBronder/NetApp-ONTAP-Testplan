################################################################################
#
# Title:        BSS-03-09.yml
# Author:       NetApp Inc. (badrian)
# Date:         2023-08-01
# Description:  Create a snapshot
#
# URLs:         https://docs.netapp.com/us-en/ontap/index.html
#               https://galaxy.ansible.com/netapp/ontap
#
# Built-in help:
# ansible-doc netapp.ontap.na_ontap_snapshot
#
################################################################################

- hosts:              "{{ ontap_primary_cluster }}"
  name:               BSS-03-09
  gather_facts:       false
  vars:
    input: &input
      username:       "{{ ontap_admin_user }}"
      password:       "{{ ontap_admin_password }}"
      https:          true
      validate_certs: false
      use_rest:       "always"
  vars_files:
    - vars/vars_labondemand.yml
  collections:
    - netapp.ontap

  pre_tasks:
  - name: Get all snapshots
    netapp.ontap.na_ontap_rest_info:
      gather_subset:
        - storage/volumes/snapshots
      owning_resource:
        svm_name:               "{{ ontap_primary_nas_svm }}"
        volume_name:            "{{ ontap_03_volume_name }}"
      use_python_keys:          true
      hostname:                 "{{ ontap_hostname }}"
      <<: *input
    register: pre_ontap

  - name: Show all snapshots on volume "{{ ontap_03_volume_name }}"
    ansible.builtin.debug:
      msg: "{{ item }}"
    loop: "{{ pre_ontap.ontap_info.storage_volumes_snapshots.records }}"

  tasks:
  - name: Create a test snapshot on volume - "{{ ontap_03_volume_name }}"
    netapp.ontap.na_ontap_snapshot:
      state:                      present
      snapshot:                   "{{ ontap_03_snapshot_name }}"
      vserver:                    "{{ ontap_primary_nas_svm }}"
      volume:                     "{{ ontap_03_volume_name }}"
      hostname:                   "{{ ontap_hostname }}"
      <<: *input

  post_tasks:
  - name: Get all snapshots
    netapp.ontap.na_ontap_rest_info:
      gather_subset:
        - storage/volumes/snapshots
      owning_resource:
        svm_name:               "{{ ontap_primary_nas_svm }}"
        volume_name:            "{{ ontap_03_volume_name }}"
      use_python_keys:          true
      hostname:                 "{{ ontap_hostname }}"
      <<: *input
    register: ontap

  - name: Show all snapshots on volume "{{ ontap_03_volume_name }}"
    ansible.builtin.debug:
      msg: "{{ item }}"
    loop: "{{ ontap.ontap_info.storage_volumes_snapshots.records }}"