################################################################################
#
# Title:        revert_BSS-06.yml
# Author:       NetApp Inc. (badrian)
# Date:         2023-08-02
# Description:  Revert configuration steps for BSS-06
#
# URLs:         https://docs.netapp.com/us-en/ontap/index.html
#               https://galaxy.ansible.com/netapp/ontap
#
################################################################################

- hosts:              "{{ ontap_primary_cluster }}"
  name:               revert_BSS-06
  gather_facts:       false
  vars:
    input: &input
      username:       "{{ ontap_admin_user }}"
      password:       "{{ ontap_admin_password }}"
      https:          true
      validate_certs: false
      use_rest:       "always"
  vars_files:
    - vars/vars_labondemand.yml
  collections:
    - netapp.ontap

  tasks:
  - name: Delete test share
    netapp.ontap.na_ontap_cifs:
      state:                      absent
      name:                       "{{ ontap_06_share_name }}"
      path:                       "/{{ ontap_06_win_vol_name }}"
      vserver:                    "{{ ontap_primary_nas_svm }}"
      hostname:                   "{{ ontap_hostname }}"
      <<: *input

  - name: Delete FlexVols
    netapp.ontap.na_ontap_volume:
      state:                      absent
      name:                       "{{ item }}"
      vserver:                    "{{ ontap_primary_nas_svm }}"
      wait_for_completion:        True
      hostname:                   "{{ ontap_hostname }}"
      <<: *input
    loop:
    - "{{ ontap_06_lin_vol_name }}"
    - "{{ ontap_06_win_vol_name }}"

  - name: Delete export policy (aka "storage group")
    netapp.ontap.na_ontap_export_policy:
      state:                      absent
      name:                       "{{ ontap_06_policy_name }}"
      vserver:                    "{{ ontap_primary_nas_svm }}"
      hostname:                   "{{ ontap_hostname }}"
      <<: *input
